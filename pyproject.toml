[project]
name = "toku-ai-mcp"
version = "0.1.0"
description = "MCP server for TokuEyes AI models and infrastructure"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.2.0",
    "python-docx>=1.1.2",
    "requests>=2.31.0",
]
